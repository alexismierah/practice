export const metadata = {
  title: "About Us",
  alternates: {
    canonical: "/about",
  },
}

export default function AboutLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
