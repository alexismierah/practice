export const metadata = {
  title: "Small Plants | Rich Haven Artificial Garden",
}

export default function ProductsServicesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
